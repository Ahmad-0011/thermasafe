# دليل ربط زر الطوارئ (ESP32) بتطبيق ThermaSafe — خطوة بخطوة

الهدف: عند ضغط العامل على الزر الموصول بـ ESP32، يظهر تنبيه SOS فوراً في تطبيق مختص السلامة.

---

## 🧰 المكوّنات المطلوبة
- لوحة **ESP32** (أي نوع: ESP32 DevKit V1 وغيره)
- **زر ضغط** (Push Button)
- **سلكان توصيل** (Jumper wires) + لوحة تجارب (Breadboard) — اختياري لكن أسهل
- كيبل **USB** لتوصيل ESP32 بالكمبيوتر
- (اختياري) LED + مقاومة 220Ω للتأكيد البصري — أو استخدم LED المدمج

---

## 1️⃣ تثبيت Arduino IDE
1. حمّل البرنامج من: **https://www.arduino.cc/en/software**
2. ثبّته وافتحه.

## 2️⃣ إضافة دعم لوحة ESP32
1. من القائمة: **File → Preferences**
2. في خانة **Additional boards manager URLs** الصق هذا الرابط:
   ```
   https://espressif.github.io/arduino-esp32/package_esp32_index.json
   ```
3. اضغط **OK**.
4. افتح **Tools → Board → Boards Manager**
5. ابحث عن **esp32** (من Espressif) → اضغط **Install** (ينزّل ~200MB، انتظر حتى يكتمل).

## 3️⃣ توصيل الدائرة الكهربائية
| الطرف | يوصل إلى |
|-------|----------|
| طرف الزر الأول | **GPIO 4** في ESP32 |
| طرف الزر الثاني | **GND** في ESP32 |

- لا تحتاج مقاومة — الكود يفعّل المقاومة الداخلية (`INPUT_PULLUP`).
- إذا الزر له 4 أرجل: استخدم رجلين **متقاطعتين** (على زاويتين متقابلتين).
- (اختياري) LED: الطرف الطويل → GPIO 2 عبر مقاومة 220Ω، والقصير → GND. أو اكتفِ بالـ LED المدمج.

```
   ESP32
 ┌─────────┐
 │  GPIO4  ├──────┐
 │         │     [ زر ]
 │   GND   ├──────┘
 └─────────┘
```

## 4️⃣ فتح الكود وتعديله
1. افتح ملف **`esp32_sos_button.ino`** في Arduino IDE.
2. عدّل هذه القيم فقط في أعلى الملف:
   - `WIFI_SSID` = اسم شبكة الواي فاي
   - `WIFI_PASS` = كلمة مرور الشبكة
   - `WORKER_SERIAL` / `WORKER_NAME` / `WORKER_LOC` = هوية العامل/الموقع
   > رابط Supabase والمفتاح **جاهزان مسبقاً** — لا تغيّرهما.
3. ⚠️ مهم: ESP32 يدعم شبكات **2.4GHz فقط** (ليس 5GHz). تأكد أن شبكتك 2.4GHz.

## 5️⃣ اختيار اللوحة والمنفذ
1. وصّل ESP32 بالكمبيوتر عبر USB.
2. **Tools → Board → ESP32 Arduino → ESP32 Dev Module**
3. **Tools → Port** → اختر المنفذ الظاهر (مثل COM3).
   - إن لم يظهر منفذ: ثبّت تعريف **CP2102** أو **CH340** (حسب رقاقة USB في لوحتك).

## 6️⃣ رفع الكود
1. اضغط زر **Upload** (سهم ➡️ في الأعلى).
2. إذا توقّف عند "Connecting…": **اضغط مطوّلاً زر BOOT** في ESP32 أثناء الرفع ثم أفلته.
3. انتظر رسالة **Done uploading**.

## 7️⃣ المراقبة والتأكد
1. افتح **Tools → Serial Monitor** واضبط السرعة على **115200**.
2. المفروض يظهر: `✅ متصل. IP: …` ثم `جاهز. اضغط زر الطوارئ…`
3. اضغط الزر → يظهر: `📡 إرسال SOS — كود الاستجابة: 201` ثم `✅ وصل التنبيه للتطبيق.`

---

## 8️⃣ تحديث التطبيق على Vercel (لتفعيل التنبيه الصوتي)
حدّثتُ `index.html` ليُصدر **تنبيهاً بارزاً + صوتاً** عند وصول النداء:
1. افتح مستودعك: `https://github.com/Ahmad-0011/thermasafe`
2. اضغط على ملف **index.html** → أيقونة القلم ✏️ (Edit) → أو احذفه وارفع النسخة الجديدة.
   - الأسهل: **Add file → Upload files** واسحب `index.html` الجديد → **Commit changes** (سيستبدل القديم).
3. Vercel ينشر التحديث تلقائياً خلال ~30 ثانية.

## 9️⃣ الاختبار الكامل
1. افتح تطبيقك على Vercel من الجوال أو الكمبيوتر.
2. اختر **مختص سلامة** → دخول (`specialist` / `1234`) → ابقَ على لوحة التحكم.
3. اضغط زر الطوارئ على ESP32.
4. خلال ثانية: 🆘 تنبيه + صوت، وتتحدّث بطاقة SOS باسم العامل والموقع. ✅

---

## 🔧 حل المشكلات الشائعة
| المشكلة | الحل |
|---------|------|
| لا يظهر منفذ COM | ثبّت تعريف CP2102 / CH340، وجرّب كيبل USB آخر (بعض الكيبلات شحن فقط) |
| يعلّق عند Connecting | اضغط زر BOOT مطوّلاً أثناء الرفع |
| لا يتصل بالواي فاي | تأكد أنها 2.4GHz، وصحّة الاسم/كلمة المرور |
| كود الاستجابة ليس 201 | تأكد أن schema.sql شُغّل بنجاح (جدول sos_alerts + سياسة الإدخال) |
| التنبيه لا يظهر بالتطبيق | تأكد أن التطبيق مفتوح ومتصل بـ Supabase (شريط "🟢 متصل") |

---

## 🔌 تستخدم ESP8266 بدل ESP32؟
في نهاية ملف الكود شرح: بدّل 3 أسطر الترويسات فقط، وبقية الكود يعمل كما هو.
